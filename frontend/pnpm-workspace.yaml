packages:
  - .

# pnpm 11 统一在工作区配置依赖覆盖；锁定已修复安全公告的 nanoid。
overrides:
  nanoid: 3.3.18
  postcss: 8.5.23

# Vite 仅需要官方 esbuild 安装其平台二进制；core-js 的赞助提示脚本明确禁用。
allowBuilds:
  core-js: false
  esbuild: true
