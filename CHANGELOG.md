# 更新记录

本项目按 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构记录用户可感知变更。PartyOps 当前使用三段式版本号；正式 Git 标签和安装制品只有在发布门禁通过后创建。

## [1.4.4] - 2026-08-21

### 新增

- 增加协议 v2 协同凭据升级/重新授权、网络与协同管理、用户归档恢复、一次性文件打开授权、党委会六步筹备、周期会议、结构化在线文档和发展党员持久档案。
- 发展党员档案支持版本化规则、实际/法定/参考/调整日期分栏、节点提醒、统计及 Word/Excel 导出。
- macOS 原生 Mach-O 启动探针记录架构、资源、PID、退出码与 stderr；Windows/Linux 统一更精确的启动状态和诊断。

### 变更

- 页面打开、心跳和灾备同步解耦；协同 401/403 停止无限重试并保留绑定等待管理员重新授权。
- 任务日期、状态和参与人修改后立即重算通知，更新当前未读提醒并撤销失效未读项。
- 用户删除改为责任移交后的可恢复归档；角色变化立即撤销旧会话并重算权限。

### 修复

- 修复 rc.9 后 UOS/麒麟 ARM64 旧协同绑定被 403 拒绝时桌面启动被灾备链路拖死的问题。
- 修复文件打开授权重启即失效、错误原因混淆、PDF 空白/WPS 空白预览和一次性令牌并发重复兑换。
- 修复 Win7 自启动注册被策略拒绝时误报整个模式恢复失败，以及任务改期后新旧通知并存。

## [1.4.3-rc.9] - 2026-08-20

### 跨平台安装与启动

- 修复统信旧配置把失效局域网 IP 用作监听地址导致端口 `18766` 绑定失败；监听地址与对外地址现在分别迁移和校验。
- 个人模式端口 `18775` 被未知程序占用时自动选择受控回环备用端口；只有 PID、实例锁和可执行路径三项一致时才接管旧 PartyOps 进程。
- Windows 主机服务、协议注册与覆盖安装均按所有权证据处理，修复 `LEGACY_SERVICE_CONFLICT`、`PROTOCOL_REGISTRY_DENIED`、Win7 KB 等效补丁误判与 Win11 空白页。
- 针对 rc.8 在部分 Win7 电脑出现个人进程 `CHILD_EXITED`、但反馈方未提供 `launcher.log` 的情况，rc.9 不再只做构建机和 PE 静态检查：安装文件释放后会在目标电脑真实启动同一冻结主程序，执行 RSA/Fernet、SQLite 安全版本与 FTS5、数据库迁移、健康端点、运行版本及首页入口检查；任一步提前退出都会回滚安装并把子进程输出写入安装日志，避免安装成功后双击才发现无响应。缺少原始日志时不虚构唯一根因，后续若取得日志仍按新的证据定点修复。
- DEB/RPM 桌面入口、换行、权限与安装后事务统一验证，修复 `PACKAGE_DESKTOP_ENTRY_INVALID`、版本错配和 `LAUNCH_LOCK_TIMEOUT`。
- 麒麟/UOS 桌面入口统一经 `/bin/bash` 进入受控启动器，缺失 `HOME` 时从账号数据库恢复；安装器在桌面用户 DBus 会话写入可信元数据并保留可执行兜底，解决安装成功但双击无反应且没有日志。
- macOS Finder 入口改为原生 C 启动探针，在冻结向导运行前先写 `launch-probe.log`；Apple Silicon 与 Intel 原生 Darwin 构建均用 LaunchServices `open -na` 完成安装后双击自检。

### 业务闭环

- 一事一档新增带权限、并发版本与审计记录的“回退到此版”，通过创建新版本保留完整历史。
- 文档管理中心恢复“操作目录”，主机可纳管目录、协同机可共享本机文件夹，无桌面能力时显示明确说明。
- 党员发展时间节点结果恢复标准 DOCX 导出，使用稳定 DOM 下载与延迟清理，兼容旧 WebView/Chromium。

### 交付边界

- 九个主安装包均重新冻结；后端行覆盖率 `95.14%`、分支覆盖率 `90.00%`，前端和官网覆盖率均超过正式门禁。
- Windows 安装器仍无 Authenticode 商业签名；macOS PKG 使用 ad-hoc 签名且未获 Developer ID/Apple 公证，首次打开必须按官网 Gatekeeper 步骤操作。
- Windows 7、国产 Linux 与 macOS 用户真机交互验收仍未取得；自动化、QEMU 与原生构建主机门禁不能替代对应用户设备验收。

## [1.4.3-rc.8] - 2026-08-19

### 国产 Linux 安装与启动

- 主程序、协同端、配置向导和更新器改为共用固定运行时，避免安全中心把临时解包的共享库识别成未授权程序并阻止启动。
- 桌面入口在启动前立即创建日志；配置目录异常时切换到当前用户的后备诊断目录，不再出现弹窗给出路径但目录中没有日志文件。
- 进程提前退出、PID 文件缺失、系统服务停止和健康检查超时分别显示稳定中文诊断码，并把桌面、主程序、业务日志、进程、端口和 systemd 状态汇总到同一诊断文件。
- 连续双击只启动一个配置向导；第二次启动复用已经就绪的页面，避免多个后台进程相互覆盖。
- 配置工具和健康检查的慢启动等待上限统一为 180 秒；明确失败仍立即结束，不用超时掩盖真实原因。
- 桌面入口会核对健康端点实际运行版本；升级前残留进程不再被误判为新版本并打开混合页面。

### Windows 启动兜底

- Windows 配置向导允许老旧设备最多启动 180 秒；无控制台桌面入口遇到未预料异常时改为显示中文诊断码并写入紧急日志，不再黑窗闪退。

### 构建与成品门禁

- Linux 构建与封装统一使用已校验的 Python 3.11 和离线依赖，不再受构建机全局 Python 环境影响。
- 最终 DEB/RPM 必须验证四个程序入口、共享库权限、固定运行时、健康端点、桌面打开及启动失败诊断；任一环节失败即拒绝发布。
- Linux 安装后事务同时核对载荷版本与健康端点实际版本，防止“包管理器安装成功但运行的仍是旧进程”。

### 银河麒麟 V10 SP1 系列适配

- 银河麒麟桌面 V10 SP1 2107/2203/2303/2403/2503 统一按处理器架构选包，不再按 CPU 型号重复制作安装包。
- 飞腾 D2000/FT-2000、麒麟 9000C/9006C/990、鲲鹏等 `aarch64` 电脑共用 ARM64 DEB；海光、兆芯、Intel、AMD 等 `x86_64` 电脑使用 AMD64 DEB。
- 官网新手下载助手、安装教程和独立兼容说明均要求以 `uname -m` 为最终依据，并继续标注国产 Linux 未完成对应真机运行验收。

### macOS 11+ 双架构适配

- 新增 Apple Silicon ARM64 与 Intel x86_64 原生 PKG，配置向导、个人模式、主机模式、协同模式、中文 OCR 与本地 LLM 均随单一安装包交付。
- 桌面入口与核心进程使用不会在默认 APFS 上发生大小写碰撞的独立名称，避免安装成功后 Finder 双击无响应。
- App 资源使用标准 Bundle 布局；嵌套 OCR、本地 LLM 与 Python 运行时先逐项签名，最后签署 Finder 主入口和整个 App，避免全新运行时令安装包在签名阶段形成不完整状态。
- 双架构候选包在对应原生构建环境执行全新安装、覆盖安装、Bundle 结构、SQLite/FTS5、OCR、本地 LLM、桌面入口和后台入口自检。当前没有 Developer ID 与公证，也尚未完成用户真机交互验收，仍属于未签名测试候选。

## [1.4.3-rc.7] - 2026-08-19

### Windows 启动与首次配置

- 修复个人模式填写管理员信息期间进程退出后出现 `WinError 10061`：提交前重新确认同版本个人进程健康；连接结果不明确时绝不自动重发账号、密码和一次性令牌，而是显示中文原因并由用户明确重试。
- 覆盖升级后若旧个人进程仍占用端口，只终止由当前数据目录进程标记和可执行路径共同证明归属 PartyOps 的旧进程，再启动新版本；未知端口占用者仍拒绝接管。
- 桌面启动器、配置向导和数据清理工具改为无控制台 GUI 入口；个人后台进程使用无窗口方式启动，解决 Windows 10/11 双击后黑框闪烁并静默退出。
- 覆盖安装读取受保护的当前模式标记；个人模式升级会停止旧版遗留的 `PartyOpsHost`、恢复为手动启动并保持更新服务自动运行，避免主机端口与个人端口并存、浏览器版本错配或局域网意外暴露。
- 启动器等待配置页面真实生成后才打开浏览器；配置损坏、进程提前退出、浏览器关联失败或协同准备超时均显示中文诊断码与日志位置，不再把英文 traceback 直接弹给新手。
- 浏览器入口带当前运行版本指纹；旧页面遇到新服务时自动强制刷新一次，持续不一致时停止刷新并展示恢复步骤，解决“页面 rc.5、服务 rc.6”一类混合版本提示。
- 覆盖安装遇到“旧主机服务仍登记、服务管理程序却已损坏或被隔离”时，会先用服务路径、历史安装位置和产品标识三重核验归属，再由 Windows 服务控制器停止并原位修复；不再要求先手工卸载，且不会接管同名第三方服务。
- 重试主机启动时忽略上一次启动残留的终止状态，只采信本次启动后写入的新诊断；模式切换回滚会回读原服务是否真正恢复运行，失败时保留回滚快照而不再误报成功。

### Windows 7

- 修复 Win7 Python 3.8 与新版 Starlette 静态文件 ETag 参数不兼容导致首页返回 500；x64/x86 均已用 Python 3.8 源码运行时验证首页和健康端点返回 200。
- 安装前不再依赖注册表中是否恰好保留 `KB2533623` 名称，而是按微软建议检测安全 DLL 加载 API；已经安装后续汇总更新的 Win7 不再被误拦截，缺少真实能力的系统仍会停止安装并给出中文步骤。
- Win7 专用载荷继续拒绝导入 `api-ms-win-core-path-*`；Windows 10/11 通用安装器仍不允许在 Win7 安装，避免误用运行时导致缺少系统 DLL。

### 麒麟、UOS、deepin 与 openEuler

- 修复 Linux PID 文件被系统复用后误把其他进程当作 PartyOps、从而双击无反应的问题；启动前同时核对 PID、进程状态和真实可执行路径，绝不终止身份不明的进程。
- 个人或用户主机进程启动后立即退出时，桌面启动器会立刻显示 `CHILD_EXITED` 与日志位置，不再盲等两分钟；升级后打开页面也会使用新的入口指纹，避免复用旧版首页。
- DEB 安装指南先把文件复制到系统临时目录，避免下载目录权限触发 `_apt` 沙盒警告；包配置失败会直接输出 `PACKAGE_*` 诊断码、失败阶段、日志尾部和安全恢复命令。
- 更新服务启用、启动和升级后主机恢复分别记录真实 systemd 输出；失败仍保持主机停止，不会用“安装成功”掩盖未通过的健康检查。
- DEB/RPM 安装后配置改为同一事务入口，依次执行运行时、静态资源、健康端点、systemd 与服务启用检查；失败会保留稳定诊断码和可执行恢复步骤，解决图形软件中心在 95% 处只显示“软件包操作异常”的问题。
- 系统内更新只自动修复 PartyOps 自己的半配置状态；若检测到其他系统软件包也未完成配置，会停止并给出诊断，不再用全局 `dpkg --configure -a` 改动无关软件。

### 事项办理

- 事项负责人、创建人或管理员现在可以在详情页删除误建事项；删除采用软删除并保留审计记录，同时使用版本条件防止覆盖其他电脑刚完成的修改。

### 已知边界

- 本版本仍是未签名候选版。Windows 7 与国产 Linux 尚无对应真机运行结论；Win7 只建议在受控局域网使用。

## [1.4.3-rc.6] - 2026-08-18

### Windows 安装兼容

- 修复部分 Windows 10/11 电脑安装时出现 `[INSTALL_DIR_CHECK_FAILED]`：安装目录校验脚本改为 Windows PowerShell 5.1 可确定识别的 UTF-8 BOM，避免简体中文系统按 GBK/ANSI 错误解码中文注释并产生语法错误。
- 安装器把校验脚本的标准输出与错误输出完整写入 Inno 安装日志；即使脚本在解析或参数绑定阶段失败，也能保留退出码和真实原因，不再只显示笼统提示。
- 新增构建期编码硬门禁和 Windows PowerShell 5.1 中文/空格路径回归，最终封装后还会从 EXE 重新提取脚本复测，防止源码正确但安装器内载荷错误。

### Windows 7 选包与运行时

- Windows 10/11 通用安装器明确要求 Windows 10 或更高版本；在 Windows 7 上会用中文提示改下 `windows7_amd64` 或 `windows7_x86` 专用安装包，避免 Python 3.11 运行时触发 `api-ms-win-core-path-l1-1-0.dll` 缺失。
- Win7 专用包继续使用隔离的 Python 3.8 Legacy 运行时，并对最终 EXE、DLL、PYD 全量检查 PE 架构、6.1 子系统及 Win8/10 专属导入；禁止通过网上下载单个系统 DLL 的方式绕过兼容性问题。
- Windows 7 安装前仍检查 SP1、KB2533623 和 Universal CRT。由于本轮没有 Win7 真机，制品保持“未真机验证”，仅建议在受控局域网使用。

### 国产电脑下载与麒麟 ARM64

- 修复麒麟 ARM64 安全中心反复提示阻止 `libgcc_s.so.1` 启动：冻结载荷不再继承 wheel/归档中的宽松执行位，共享库与静态资源统一为 `0644`，只有受控程序入口保留执行权限。
- 主机与更新服务连续失败三次后停止自动重试并保留中文日志，避免底层安全策略拒绝时形成无限弹窗循环。
- 修复麒麟/UOS 安装成功后双击无反应：桌面启动器不再依赖隐藏进程自行打开浏览器，而是等待配置工具或健康端点真正就绪后再交给系统浏览器；浏览器关联失败会显示中文原因和日志位置。
- Linux 个人、主机、协同三种模式统一使用受控桌面启动链；修复旧发行版 Bash 4.2 首次无参数启动直接退出、个人模式重登读取错误配置、个人配置被错误强制迁移到 HTTPS，以及协同页面令牌无法回传给桌面进程的问题。
- Windows 个人、主机和协同桌面入口同样核对页面交接结果；默认浏览器关联损坏或协同进程超时时显示中文弹窗与可复制地址，不再静默退出。
- Windows 冻结门禁显式收集 `backports.tarfile` 并真实启动最终配置向导，修复 PyInstaller 仅封装成功、安装后向导因缺少 vendored 模块立即退出的隐患。
- DEB/RPM 安装后自检新增 Bash 语法、桌面入口和 `TryExec` 绑定校验；AMD64/ARM64 制品还必须通过首次配置、个人、主机、协同及浏览器失败五场景启动仿真。
- 官网新增两步新手下载助手，先选系统，再按飞腾/鲲鹏或海光/兆芯/Intel/AMD 等处理器品牌给出唯一安装包；架构不明、龙芯 LoongArch 与 RISC-V 不提供猜测性下载按钮。

### 发布

- 七个平台安装包、应用内更新包、机器可读清单、SBOM、VEX、README、官网与更新日志统一提升为 `1.4.3-rc.6`；线上只挂载本版本安装包。
- 修正 Python 项目元数据和锁文件仍残留 rc.4 的内部版本，并新增冻结前一致性门禁；后端、协同端、Python 元数据、锁文件、业务前端与官网任一版本不一致都会拒绝构建。

## [1.4.3-rc.5] - 2026-08-18

### Windows 安装修复

- 发布全新版本化的 rc.5 安装包，不再覆盖 rc.4 同名文件，避免浏览器、CDN 或下载工具继续命中旧安装器并显示 `INSTALL_DIR_PARENT_ACL_UNSAFE`。
- 自定义程序目录继续支持本机固定 D/E 盘、中文和空格路径。安装前只拒绝磁盘根、网络/移动盘、重解析点、非 PartyOps 非空目录等真实风险；不再因上级目录面向日常文件的通用 ACL 误拦截。
- 安装器创建目标目录后自动把 DACL 收敛为 Administrators/SYSTEM 完全控制、普通用户只读执行，并为目录和全部载荷设置高完整性标签；这使自定义位置可用，同时阻止普通桌面进程借上级目录权限替换服务程序。
- 修复安装后载荷 ACL 继承、历史数据目录权限、覆盖升级后的主机/更新服务自动启动，以及“保留业务数据卸载”残留系统运行缓存的问题。
- 增加真实自定义目录完整流程回归：宽松父目录预检、目标创建、权限保护、最终回读、覆盖升级、服务健康、保留数据卸载和全新重装必须连续通过。

### 多系统与更新

- Windows 10/11、Windows 7 SP1 x64/x86 使用同一 rc.5 安装修复源码重新构建；Win7 继续执行 Python 3.8 Legacy、app-local UCRT、Tcl/Tk、PE 导入与架构门禁，仍明确标注未真机验证。
- 七个平台安装包与七个 Ed25519 应用内更新包统一提升到 rc.5；更新目录继续按平台、包格式和 CPU 架构精确选包，失败保留数据并回滚。
- rc.5 仍是未签名候选版，不是稳定版。Windows 7 与国产 Linux 的真机运行边界不因普通 GitHub Release 而改变。

## [1.4.3-rc.4] - 2026-08-17

### 修复

- 修复 Windows 10/11 自定义程序目录的误报：管理员自己在 D/E 盘创建的中文、空格目录不再触发 `INSTALL_DIR_PARENT_UNSAFE` 或笼统的 `INSTALL_DIR_CHECK_FAILED`；真实允许普通用户替换 LocalSystem 服务文件的父目录仍以 `INSTALL_DIR_PARENT_ACL_UNSAFE` 拒绝。
- 安装目录检查把 PowerShell 的精确中文诊断写入独立文件并由安装器回读，创建前、创建后和 ACL 收敛后连续复核，避免路径解析或错误流造成错误提示丢失。
- Windows 冻结向导加入真实 Tcl/Tk 自检，构建阶段必须成功创建并销毁隐藏窗口；缺少 `_tkinter.pyd`、Tcl/Tk DLL 或脚本资源时拒绝生成安装器。

### Windows 7 Legacy

- 发布独立 Windows 7 SP1 x64/x86 安装器：x64 提供完整主机、协同、OCR、语义重排和本地 LLM；x86 提供核心主机、协同、数据库、文件、档案、备份和中文 OCR，不启用语义重排与本地 LLM。
- 使用独立 CPython 3.8 锁和可审计安全回移组件；`cryptography`、Pillow、FastAPI/Starlette 等回移保留源码补丁、证据、哈希与离线 wheelhouse 闭包，不污染 Python 3.11 主线。
- x86 固定 SQLite 3.53.4 与 Tesseract 5.5.2 静态运行时；两套架构均使用 Microsoft Windows SDK 10.0.19041 官方 app-local UCRT 完整集合，并逐文件校验 SHA-256、PE 架构、导入 API 和子系统基线。
- 安装前检查 Windows 7 SP1、KB2533623、Universal CRT、系统位数和目录权限；由于 Windows 7 已停止系统级安全维护且本轮无真机，必须仅在受控局域网使用并明确标注“未真机验证”。

### 发布

- 版本号提升为 `1.4.3-rc.4`，七个系统安装包使用不可变版本化文件名；Windows 普通用户仍只需下载一个 EXE，Linux 每台电脑只需下载一个与包格式和架构匹配的 DEB/RPM。
- 应用内更新继续使用 format v3、Ed25519、大小和 SHA-256 校验，并按 `windows`、`windows7`、`linux-deb`、`linux-rpm` 及架构精确选包。

## [1.4.3-rc.3] - 2026-08-15

### 新增

- 新增“个人使用”模式：只监听本机回环地址，不安装主机服务、不开放局域网、不要求管理员授权；需要多机协同时再切换为主机。
- 设备心跳与数据库 `0019` 记录真实系统、发行版、架构、包格式和运行能力；旧的 `platform=uos/windows` 仍可读取。
- 更新清单升级为 format v3，按 Windows/Linux、DEB/RPM 和 AMD64/ARM64 精确选包；支持后台下载、Ed25519 验签、一致性备份、原位安装、健康检查与失败回滚。
- 麒麟/UOS/deepin 使用双架构 DEB，openEuler 使用双架构 RPM；每台电脑只需下载一个与架构匹配的原生安装包。

### 修复与安全

- Windows 文件协议改为管理员范围 `Software\Classes`，写入前检测旧键/外部冲突，写入后回读验证，失败中止并回滚，不再允许“忽略后半安装”。
- 识别 `[WinError 786]`/SRP/AppLocker/WDAC 组织策略阻断，提供中文诊断 `ADMIN_POLICY_BLOCKED`；系统不尝试绕过单位安全策略。
- 卸载时可选“仅删除程序”或“彻底卸载”。彻底卸载只删除能由 PartyOps 归属标记证明的数据目录，磁盘根、系统目录、网络盘和重解析点一律拒绝。
- 主机启动异常收敛为中文诊断码，持久化子进程输出、退出码和诊断编号，不再把 Starlette/SQLAlchemy 英文栈直接抛给新手。
- Linux 离线闭包严格拒绝重复 `cryptography`、脏 wheelhouse、缺失 ARM64 语义/LLM 运行时和超出 glibc 2.17 的二进制符号。
- llama.cpp 按官方固定标签从源码在 glibc 2.17 工具链重建，关闭 OpenSSL 和本机专用指令，并验证 ELF、动态依赖、最高 GLIBC 符号和嵌入提交号。
- Linux 冻结时显式封入 Tcl/Tk 并启动四个入口自检，防止“主机能启动，但选择数据目录时崩溃”。

### 发布边界

- rc.3 仍是未签名候选版。新增国产 Linux 制品经本地构建、闭包、静态 ABI/API 与冻结运行时自检，但尚无麒麟/UOS/deepin/openEuler 真机安装结论。
- Windows 7 x64/x86 的旧运行时依赖未达到“高危/严重漏洞为零”门禁，rc.3 不发布 Win7 安装包，不用忽略规则伪造通过。
- rc.2 的发布私钥无法恢复，rc.2 到 rc.3 需最后一次手工原位升级；rc.3 之后才进入系统内快速升级链。

## [1.4.3-rc.2] - 2026-08-13

### 修复

- Windows 安装和首次配置支持中文、空格及非 C 盘自定义数据目录；UAC 提权通过 Base64 参数保留原选择，缺少 `--data-dir` 时拒绝回退到 C 盘。
- Windows 安装器改为 PartyOps 专属简体中文向导，程序安装目录与业务数据目录分步选择且升级时分别保留；卸载重装优先读取现存主机真实数据配置，不会被过期预选标记覆盖；覆盖旧文件前会等待主机和更新服务完全停止。
- 数据库、附件、备份、证书、模型、缓存、运行日志及客户端接收文件跟随所选数据根目录；升级迁移先停服务、复制并校验 SQLite，成功前不切换且不删除旧数据。
- 主机服务持续监督子进程并保留 stdout、stderr、退出码和阶段状态，新增稳定诊断码；服务仅在确认主机角色后设为自动启动。
- 主机健康检查固定走 `127.0.0.1`，局域网展示地址与监听地址分离；管理员创建只在本机健康后执行，消除 `WinError 10061`。
- 主机启用 TLS 时，健康、首次配置和管理诊断接口统一返回 `https://` 展示地址，避免就绪后又跳回明文地址。
- 向导和启动器不再打开未就绪 URL；失败时提供重试、复制诊断和打开日志，不降级为普通用户进程。
- 前端全新生产构建后验证入口、路由块、WebAssembly 与静态资源闭包，修复漏打资源和未就绪误开共同导致的空白页。

### 打包与交付

- Windows 标准交付改为单文件 `PartyOps_1.4.3-rc.2_windows_amd64.exe`；普通用户无需再下载 `.sha256` 文件，官网和 Release 直接显示最终哈希。
- UOS amd64/arm64 wheelhouse 从干净目录生成，统一 `cryptography==50.0.0`；构建前按规范化包名检查重复项、版本、架构与完整离线闭包。
- UOS rc.2 严格模式要求两个架构的本地语义和 llama.cpp 运行时完整，任一架构不完整就拒绝生成候选套件，不再先降级后继续出包。
- UOS 套件使用带 `rc.2` 的独立顶层目录，禁止覆盖合并旧 wheelhouse；重复包错误会同时列出冲突文件和干净解压建议。

### 发布边界

- rc.2 仍是未签名测试候选。当前 Windows 11 环境完成自动化与制品验收；Windows 10、UOS 原生 DEB、真实多机和长期稳定性不作虚构承诺。

## [1.4.3-rc.1] - 2026-08-11

### 新增

- “今日 → 备忘录”提供严格本机私有的文本/清单、置顶、标签、纸色、搜索、500 毫秒自动保存、30 天回收站和撤销删除。
- 备忘录支持 PBKDF2-SHA256 + AES-256-GCM 加密备份；只允许同一 PartyOps 账号显式导入，不进入主机 API、协同和主机备份。
- 新增依据 2026 年 5 月新版《中国共产党发展党员工作细则》的确定性日期引擎，区分法定截止、最早日期、建议窗口、实际日期和人工确认节点。
- 支持实际进度补录、工作日/调休、暂算标识、日期顺序和超期风险提示；Word 导出按 GB/T 9704-2012 的 A4 版心、字号、行距和页码规范排版，时间轴按阶段分表并统一使用中文日期与办理状态。
- 新增管理员“党员发展补充材料”；国家规则期限锁定，单位模板只能追加材料、责任主体和说明。上传旧表格中的“三考”、思想汇报和自传等默认停用，待管理员确认。
- 数据库迁移 `0018` 增加补充材料模板和条目；个人备忘及计算草稿仍不进入主机数据库。

### 修复与加固

- 同名补充材料模板在事务 `flush` 阶段触发唯一约束时，稳定返回 409，不再形成未处理 500。
- 主机/协同机导航继续统一使用运行时有效能力；备忘录与计算器对所有账号可见，补充材料配置仅管理员可见。
- 修复协同 Agent 未配置客户端证书路径时把当前工作目录误判为证书的问题；角色标题和冻结运行时冒烟脚本同时收口。
- 版本门禁、备份模式、Windows/UOS 打包脚本和统一更新清单升级到 `1.4.3 / 0018`。

### 验证与发布状态

- 后端 411 项、前端 126 项全量回归通过；后端行/分支覆盖率为 94.50% / 80.04%，前端为 93.26% / 80.40%，覆盖率门禁通过。
- 前端类型检查与生产构建、Python/前端依赖审计、Bandit 高风险扫描、Gitleaks 34 个提交和工作区扫描均通过。
- Windows 11 x64 冻结运行时健康检查、SQLite 3.53.4、静默安装/卸载和主要 Chrome 流程通过；Windows 10、UOS 双架构、20GB、24 小时和正式双签名仍缺真实证据，状态保持 **NO-GO / 测试候选**。

## [1.4.2-rc.1] - 2026-08-09

### 新增

- 文件中心接入 AnyDoc 0.1.7，支持 Office、OpenDocument、RTF、EPUB、CSV 等格式的浏览器本地结构化阅读。
- 接入 pdf-inspector 0.1.3，显示 PDF 类型、页数、置信度、表格/分栏页和 OCR 建议，并保留原始 PDF 预览。
- 主机和协同机文件统一阅读弹窗；远端内容完成设备中转和哈希校验后自动打开。
- 真实 Playwright 主机/协同 Agent 文档阅读与浏览器下载回归，以及专业开源 README、截图和完整安装教程。

### 修复

- 修复共享权限撤销后，已创建的远端传输仍可能继续读取内容的问题。
- 最后一块到达即过早完成，导致 Agent 源变化校验与 finalize 冲突。
- 同一根目录自动扫描和立即同步并发造成唯一约束错误。
- 远端索引统计在 flush 前计算而显示零文件。
- pdf-inspector 页标记显示为正文、PDF 类型中文显示错误。

### 安全

- 文档解析在一次性 Web Worker 中执行；禁用 Markdown HTML、外部图片和危险链接协议。
- HTML、XML、SVG 不提供可能主动联网的原始内联预览。
- 结构化阅读限制为 64 MiB 和 200 万字符；关闭弹窗即终止 Worker、请求和对象 URL。

### 发布状态

- 后端 201 项、前端 41 项、Sites 4 项、真实 Agent 浏览器 E2E 2 项通过。
- 后端覆盖率 73.86%、前端行覆盖率 5.48%，且目标平台、容量、长稳和签名门禁未完成，状态为 **NO-GO**。

## [1.4.1] - 候选版

- 真文件共享、普通用户目录发布、团队/指定成员权限、双通道下载和本地 AI 分能力激活。
- 该版本只有未签名 Windows 11 候选，不是正式 GitHub Release。

## [1.4.0] - 候选版

- 重要档案贡献权限、通知评论、“我的工作”、共享目录审批和跨平台更新适配。
- 该版本未完成 UOS 双架构和正式签名，不是正式 GitHub Release。
