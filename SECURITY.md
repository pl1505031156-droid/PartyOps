# 安全策略

## 支持范围

PartyOps 尚未发布首个通过全部门禁的稳定 Release。安全修复优先进入当前 `main` 和最新候选版本；历史 1.4.0/1.4.1 候选不承诺继续维护。

## 私密报告漏洞

请优先使用 GitHub 的 [Private vulnerability reporting](https://github.com/pl1505031156-droid/PartyOps/security/advisories/new)。报告至少包含受影响版本、操作系统/架构、主机或协同机角色、复现条件、影响、最小复现和建议缓解方式。

不要在公开 Issue、截图或附件中提供：

- 真实单位、姓名、事项、档案或共享文件；
- 用户密码、会话 Cookie、设备令牌、配对码；
- TLS/设备/更新私钥、模型包签名密钥；
- 数据库、备份、未脱敏日志或内网地址拓扑。

如果 GitHub 私密报告暂不可用，请只创建不含技术细节的公开 Issue，请求维护者启用私密联系；在获得私密渠道前不要公开利用步骤。

## 处理原则

维护者会确认问题、评估影响、准备回归和修复，并在可安全披露后更新公告。任何安全结论都必须由可重复测试支持；正式修复仍需通过项目发布门禁。

## 响应目标

| 严重程度 | 首次确认目标 | 缓解/修复目标 | 发布方式 |
| --- | --- | --- | --- |
| 严重 | 24 小时 | 72 小时内提供隔离或修复版本 | 私密通知后紧急安全版本 |
| 高 | 2 个工作日 | 7 天 | 安全版本并发布公告 |
| 中 | 5 个工作日 | 30 天 | 最近维护版本 |
| 低 | 10 个工作日 | 90 天或下个功能版本 | 变更日志说明 |

上述时间是维护目标，不是服务等级承诺；复杂供应链问题会先提供禁用、隔离或回滚方案。

## 发布者安全基线

- 不把主机浏览器端口或 Agent 端口映射到公网；只允许受控局域网。
- 正式环境必须使用主机 TLS 与 Agent 双向 TLS，设备令牌不得进入浏览器端口。
- 更新包和模型包只信任部署端预置公钥，发布私钥不得进入源码、制品或主机数据目录。
- 首任管理员在主机本机创建；演示数据默认关闭，系统不发布固定演示口令。
- 导入备份、文档、模型和更新前保留容量上限、路径/哈希/签名校验。
- 每次发布重新生成 `docs/sbom-*.cdx.json`，执行 Python/前端依赖审计并人工复核许可证变化。
- 使用固定版本 gitleaks 运行 `scripts/scan-secrets.ps1`，同时扫描 Git 历史与当前工作区。

## 支持者责任与披露

维护者不会要求报告者发送真实业务数据库、私钥或完整生产日志。确认修复后会在公告中列出受影响版本、缓解措施、修复版本和致谢（报告者可选择匿名），但不会在大多数用户完成升级前公开可直接利用的细节。
