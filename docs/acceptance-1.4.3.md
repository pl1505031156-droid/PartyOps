# PartyOps 1.4.3-rc.4 候选验收记录

最后更新：2026-08-17（北京时间，UTC+8）。结论：**源码修复与新增平台预检正在收口；最终七个文件尚未从不可变标签重建，因此当前记录不构成发布放行。**

## 已完成的本地证据

- 后端 892/892 通过，行覆盖率 95.21%、分支覆盖率 90.02%；前端 173/173 通过，行 97.17%、分支 90.07%，类型检查、静态入口与生产构建通过。
- 当前官网 rc.3 基线 33/33 通过，行覆盖率 98.39%、分支 93.30%，静态入口与生产构建通过；七包最终哈希写入 rc.4 页面后必须再次执行相同门禁。
- 主线 Python 锁一致，`pip check` 与 `pip-audit` 已知漏洞为 0；前端和官网生产依赖高危审计为 0。Ruff 致命规则、Bandit 高危结果、Git 历史及工作区秘密扫描均为 0。
- Windows 自定义程序目录继续支持本机固定磁盘、中文、空格和非 C 盘路径。真实 PowerShell 检查已覆盖 `D:\软件\PartyOps` 等安全路径，并对普通用户确实具备删除/替换权限的祖先目录返回精确诊断。
- Win7 x64/x86 使用独立 Python 3.8 锁与离线 wheelhouse；`cryptography`、Pillow 安全回移具有源码、补丁、哈希和复现证据，未使用漏洞忽略规则。
- Win7 x64 的 ONNX Runtime 与 tokenizers 已降至经二进制导入表和真实 CPU 烟雾验证的兼容版本；x86 明确关闭语义重排和本地 LLM。
- Win7 两个架构使用 Microsoft 官方 app-local Universal CRT；冻结向导必须真实创建并销毁 Tcl/Tk 隐藏窗口，PE 门禁逐项检查架构、子系统版本、导入 API、来源清单和哈希。
- Linux 个人更新已收敛为一次 root 事务：全生命周期锁、安装、降权健康检查、目标版本核验、失败回滚和清理不能被公开 CLI 拆开；polkit 使用管理员认证且限定动作参数。
- Windows 卸载、自启动清理、服务启动类型、数据迁移、模式切换、更新目录与个人模式健康检查的既有阻断项已修复并纳入回归。

本轮按用户要求不使用 Codex Security 插件，也不使用远端 CI/CD。门禁由本机源码审查、故障注入、依赖审计、真实 Inno/PyInstaller 构建、二进制检查、恶意软件扫描和浏览器回归承担；不因此降低发布标准。

## 冻结后必须补录

1. 七个最终安装包的精确大小、SHA-256、格式、资源闭包、SBOM、VEX、恶意软件与安装后自检结果。
2. `v1.4.3-rc.4` 源码提交、机器可读发布清单、format v3 更新目录和 Ed25519 验签结果。
3. 七包元数据写入后的官网测试、覆盖率、构建，以及 CloudStudio、GitHub Release 与 EdgeOne 线上文件的完整下载哈希、Magic、Content-Type、Content-Length、Range 206 和北京时间上传记录。
4. 官网桌面端、移动端、下载回退、更新日志、个人简历和微信二维码交互的在线回归结果。

任一制品存在未修复高危/严重漏洞、依赖闭包不完整、静态资源缺失、哈希不一致或安装事务失败，只阻断该制品；不能用警告或“未真机验证”替代失败。Windows 7 与国产 Linux 即使通过发布门禁，仍须公开标注未在对应真机完成运行验收。
