# PartyOps 1.4.3-rc.7 候选验收记录

最后更新：2026-08-19 15:50（北京时间，UTC+8）。
产品源码提交：`a35a522b5e62093934e81d9098bd1e25fccb68ec`

## 自动化与构建

- 后端：951 项测试通过，行覆盖率 94.00%；仅保留已记录的 Starlette/SWIG 第三方告警。
- 前端：178 项测试通过，行覆盖率 97.19%、分支覆盖率 90.11%；类型检查和生产构建通过。
- 前端冻结资源闭包：102 个入口引用资源、132 个总资源均存在，状态通过。
- 七个平台安装包均从同一产品提交构建；版本一致性、Linux shell 语法和更新矩阵测试通过。

## Windows 验收

- 当前 Windows 11 完成 rc.6→rc.7 自定义非 C 盘中文/空格目录覆盖安装；安装日志记录成功。
- 个人模式在 `127.0.0.1:18775` 返回 rc.7 健康状态，SQLite 3.53.4 与 FTS5 可用；旧主机服务保持停止/手动，更新服务保持运行/自动。
- Win7 x64 共 262 个 PE 文件、Win7 x86 共 158 个 PE 文件通过架构、子系统和禁止 API 导入门禁，不依赖 `api-ms-win-core-path-l1-1-0.dll`。
- 当前没有 Win7 真机，因此未执行 Win7 原生启动验收；Win7 仅建议在受控局域网使用。

## 国产 Linux 验收

- AMD64 DEB/RPM 与 ARM64 DEB/RPM 均从最终安装包本体解开并完成包自检、个人服务、健康端点和桌面启动器回归。
- ARM64 在 glibc 2.17 的 aarch64 QEMU/chroot 环境通过；因 QEMU 的 `/proc/<pid>/exe` 指向模拟器，模拟环境仅跳过桌面进程归属断言，真实 ARM 环境仍执行完整断言。
- 双架构离线闭包包含唯一的 `cryptography==50.0.0`；OCR、SQLite/FTS5、语义与本地运行时清单门禁通过。
- 当前没有麒麟/UOS/deepin/openEuler 真机，不宣称这些商业系统已完成原生真机验收。

## 安全门禁

- `pip-audit`、前端与官网生产依赖审计均未发现已知漏洞。
- Bandit 高/中危为 0；gitleaks 对当前代码和 106 个提交扫描未发现秘密。
- ClamAV 1.5.3（3,628,010 条签名）扫描七个安装包，感染数为 0。
- Python、前端与官网 CycloneDX SBOM 已生成；发布目录同时包含许可证、VEX 与门禁记录。

## 仍需在线完成

CloudStudio、GitHub、Gitee 与 EdgeOne 的上传、哈希/Range/HTTPS 和桌面/移动端核验必须在实际发布后记录；未完成前不得声称线上交付完成。
