# PartyOps 1.4.3-rc.8 候选验收记录

最后更新：2026-08-19 20:31（北京时间，UTC+8）。
产品源码提交：`bc0ad0c0e6eb4f272bcda731e52894dc4a282c48`

## 自动化与构建

- 后端完整测试套件通过；仅保留已记录的 Starlette/SWIG 第三方弃用告警。
- 前端 18 个测试文件、178 项测试通过；类型检查和生产构建通过。
- 最终 Windows 安装目录内前端闭包通过：102 个入口与路由引用文件全部存在，冻结目录共 192 个文件。
- 官网静态工作进程 4 项测试通过；版本一致性、Linux 安装后事务、桌面启动与更新矩阵测试通过。

## Windows 验收

- 当前 Windows 11 完成 rc.7→rc.8 自定义非 C 盘中文/空格目录真实覆盖安装；安装日志记录成功。
- 覆盖安装保留原 E 盘程序目录、业务数据库与服务启动类型：主机服务保持停止/手动，更新服务保持运行/自动。
- 个人模式在 `127.0.0.1:18775` 返回 rc.8 健康状态，SQLite 3.53.4 与 FTS5 可用；重复启动仍只有一个归属明确的 PartyOps 进程。
- 实际业务库 `integrity_check=ok`，数据库迁移版本为 `0019`，116 张表可读。
- Win7 x64 共 262 个 PE 文件、Win7 x86 共 158 个 PE 文件通过架构、子系统和禁止 API 导入门禁；x64/x86 专用包随附 UCRT/API-set，不依赖 `api-ms-win-core-path-l1-1-0.dll`。
- 当前没有 Win7 真机，因此未执行 Win7 原生启动验收；Win7 仅建议在受控局域网使用。

## 国产 Linux 验收

- AMD64 DEB/RPM 与 ARM64 DEB/RPM 均从最终安装包本体解开并完成包自检、个人服务、健康端点和桌面启动回归。
- AMD64 在 glibc 2.17 环境执行真实二进制；ARM64 在 glibc 2.17 的 aarch64 QEMU/chroot 环境执行同等动态门禁。
- ARM64 透明仿真无法等价验证真实 ARM 内核的 `/proc/<pid>/exe` 归属，因此该项只在真实设备安装后执行。
- 双架构离线闭包包含唯一的 `cryptography==50.0.0`；OCR、SQLite/FTS5、语义、本地 LLM 和前端资源门禁通过。
- Linux 安装后事务覆盖自检、服务启用、升级恢复与失败诊断；桌面入口覆盖首次配置、个人、主机、协同与浏览器失败路径。
- 当前没有麒麟/UOS/deepin/openEuler 真机，不宣称这些商业系统已完成原生真机验收。

## 安全门禁

- `pip-audit`、前端与官网生产依赖审计均未发现已知漏洞。
- Bandit 高/中危为 0；gitleaks 对当前代码和 112 个提交扫描未发现秘密。
- ClamAV 1.5.3（3,628,002 条签名）扫描七个安装包，读取 1.20 GiB，感染数为 0。
- 构建机 Windows Defender 服务未启用，因此未冒充 Defender 扫描通过；已在安全门禁记录中保留该限制。
- Python、前端与官网 CycloneDX SBOM、VEX 和门禁记录已生成。

## 仍需在线完成

CloudStudio、GitHub、Gitee 与 EdgeOne 的上传、哈希、Range 206、HTTPS 和桌面/移动端核验必须在实际发布后记录；未完成前不得声称线上交付完成。
