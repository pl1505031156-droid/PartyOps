# PartyOps 1.4.3-rc.7 候选验收记录

最后更新：2026-08-19（北京时间，UTC+8）。当前结论：**rc.7 尚未冻结，禁止上传或对外宣称已经通过验收。**

## 当前状态

- 本轮正在修复 Windows 首次配置、模式切换、浏览器与服务版本不一致、Win7 兼容、国产 Linux 安装后无法启动和事项删除等现场问题。
- 源码、七个平台安装包、七个应用内更新包、发布清单、SBOM、VEX 和官网信息必须在同一个最终提交上重新生成；不得把 rc.6 制品改名为 rc.7。
- 现有 rc.6 安装与运行记录只作为历史回归基线，不能作为 rc.7 放行证据。

## rc.7 发布阻断门禁

- 后端、前端、官网、安装器与更新器测试全部通过，行与分支覆盖率达到发布标准。
- Windows 10/11 在本机完成全新安装、覆盖升级、自定义中文/空格目录、个人模式、主机模式、首次管理员和卸载回归。
- Win7 x64/x86 的独立 Python 3.8 冻结运行时通过 PE、导入 API、静态资源和健康端点门禁，最终载荷不得导入 `api-ms-win-core-path-l1-1-0.dll`。
- 四个 Linux 包必须从最终安装包本体执行架构、依赖闭包、postinst/scriptlet、systemd、桌面启动器和健康端点验证；安装后自检失败必须留下中文诊断并保持服务停止。
- 七个应用内更新包必须通过 Ed25519、大小、SHA-256、平台与架构矩阵校验，并验证失败回滚。
- 生产依赖严重/高危漏洞为零；秘密扫描、SAST、许可证、SBOM、归档路径穿越、二进制导入表和恶意软件扫描通过。
- 最终制品的提交、文件大小、SHA-256、测试时间和未验证项必须由真实输出写回本文件和机器可读发布清单。

## 已知环境边界

- 当前没有 Windows 7、麒麟、UOS、deepin、openEuler 真机，因此不得宣称这些系统已完成原生真机验收；静态、兼容环境或用户态验证必须明确标注。
- 当前没有 Authenticode 商业代码签名证书，Windows 安装器仍可能触发 SmartScreen，发布页必须提供 SHA-256。
- Windows 7 已停止系统级安全维护，只建议在受控局域网使用；不得引导用户从第三方网站下载单个系统 DLL。

完成全部门禁后，再将真实验收结果、最终提交和制品哈希原样写回；在此之前，本文件不得作为发布批准书。
