# 验收与发布记录

最后更新：2026-07-29

## 本地已自动化验证

- Vue 类型检查与 Vite 生产构建。
- 后端状态机、日期解析、路径安全、哈希、登录、会话撤销。
- 任务闭环、审核退回、409 冲突草稿、材料终稿、归档门槛。
- 敏感事项默认最小化、FTS5 搜索、Word/Excel、迎检包。
- 备份清单、损坏检测、管理员下载和终端只读配对拉取。
- 两个独立登录会话完成创建、分工、等待反馈、送审、退回、补终稿、通过、归档；第二端 SSE 更新约 1 秒可见。
- 标题搜索与终稿文件名搜索、规范归档包、迎检汇总包均完成浏览器实测。
- 手动备份、哈希校验、终端首次拉取、304“已是最新”、令牌撤销、在线恢复和恢复前自动备份均完成实测。
- 主机进程重启后登录会话、归档事项、终稿、评论和全过程留痕保持可用。
- 终端伴随进程只读取未读数量和修订号，在免打扰时段外调用统信标准桌面通知，不取得任务标题或正文。
- 设备中心已验证上限 1—20、一次性入网、共享根审批、设备开关、联合搜索、主机中转和证书轮换命令；20 台并发与 D2000/8 原生 Agent 需在 UOS 现场复核。
- 更新中心已验证清单、签名、架构选择、主机/指定/全部终端队列和失败回滚代码路径；真实 `.deb` 原位升级需在目标机执行。
- 10,000 条事项本机冒烟：工作首页 p95 443.69 ms，FTS5 搜索 p95 0.92 ms。
- 100,000 个文件索引本机冒烟：全文搜索 p95 1.15 ms，按需加载首屏 p95 0.94 ms。
- 隔离 `.run-e2e` 主机浏览器实测：Ctrl+K 全局搜索命中事项；帮助中心八章教程可切换并保留上手清单；事项勾选、批量处理弹窗、智能文件夹筛选正常；效率中心专题/规则/日历/文档核对/报告设计/AI 审批标签可切换，规则对话框可打开；截图证据见 `output/playwright/efficiency-qa/`。
- 生产前端重新执行 `pnpm run test`（7 passed）、`pnpm run typecheck`（通过）和 `pnpm run build`（通过）；后端 `.venv` 执行 `pytest backend/tests -q`（73 passed）。
- 本轮后端 116 项自动化测试通过，全量覆盖率约 73%；低于最终发布目标 90%，主要缺口集中在设备 Agent、更新执行器、多设备接口和 UOS 专用错误分支。该数字是现场补测前的诚实基线，不把“测试全绿”误写成“所有分支已覆盖”。
- 前端 16 项 Vitest 通过；修正统计范围覆盖全部页面后，语句覆盖率 3.83%、分支 60.9%、函数 40.67%。页面组件当前主要依靠生产构建和应用内浏览器回归，覆盖率门槛仍会阻止正式发布。

自动化证据由后端 `.venv\\Scripts\\python.exe -m pytest`、前端 `pnpm` 测试/构建和 Playwright 隔离主机生成；覆盖率报告位于 `backend/htmlcov/`。

## 目标机待执行矩阵

| 编号 | 场景 | 验收标准 |
| --- | --- | --- |
| UOS-01 | UOS V20 amd64 与 ARM64 便携包启动 | 无 Node、无外网；健康检查正常 |
| UOS-02 | `.deb` 安装/升级/卸载 | 数据目录不被卸载脚本删除 |
| COL-01 | 多浏览器上下文与设备 Agent 协作 | 已在 Windows 局域网地址通过；UOS amd64 + ARM64 及多终端复核 |
| CON-01 | 同版本并发修改 | 仅一个成功，另一个 409 且有草稿 |
| OFF-01 | 断开互联网 | 全部核心流程可用 |
| OFF-02 | 终端断开主机后重连 | 自动补拉最新灾备副本 |
| RES-01 | 损坏备份 | 自动化测试已通过；UOS 复核 |
| RES-02 | 恢复中断 | 自动化回滚测试已通过；UOS 复核 |
| DR-01 | 主机故障，终端副本切换 | 单一新主机恢复，旧主机保持停机 |
| PERF-01 | 10,000 条任务首页 | Windows 本机 443.69 ms；UOS 复核 |
| PERF-02 | 10,000 条任务搜索 | Windows 本机 0.92 ms；目标为 ≤1,000 ms |
| PERF-03 | 100,000 个文件全文搜索/首屏 | Windows 本机 1.15/0.94 ms；UOS 复核 |

## 发布门槛

- `design-qa.md` 最终为 `passed`。
- 本地构建、类型检查和功能测试零错误，后端含分支覆盖率达到 90%。
- 生产 SQLite ≥ 3.51.3 且 FTS5 可用。
- 目标 UOS 构建、多设备（含 amd64 + ARM64）、无外网、重启、灾备切换全部通过。
- 生成制品 SHA-256，并记录构建机 UOS、内核、glibc、Python、SQLite 版本。

当前状态：功能测试、类型检查、生产构建、19 个一级路由和 Product Design 视觉验收通过；覆盖率门槛、UOS 双架构默认程序打开、三机协同和连续运行验收未通过前，只允许试装验证，不标记生产发布完成。
