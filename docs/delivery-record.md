# 交付记录

## 前置说明

- 输入：`基层党建工作闭环协同系统_Codex开发提示词.docx`、`基层党建工作闭环协同系统_Codex开发提示词_减负版.docx` 和用户确认的方案 1 实施计划。
- 假设：两人可信局域网、一台主机为唯一真相、无公网访问；主机可为 UOS V20/Hygon amd64，协同终端可为 UOS V20/飞腾 D2000 ARM64。
- 时效：代码、依赖和本机验证最后更新于 2026-08-04。
- 不确定性：当前机器为 Windows，没有 UOS V20/Hygon 构建环境；因此不声明 Linux 二进制已完成目标机验收。

## 交付内容

- Vue/FastAPI 完整源码、Alembic 迁移、自动化测试、性能脚本。
- 本地解析、任务闭环、材料版本、搜索导出、模板周期、知识联系人、权限审计。
- LAN SSE/轮询、脱敏桌面提醒、终端备份拉取、恢复回滚、诊断和日志。
- UOS 一键环境补齐、静态 SQLite/PyInstaller、便携包与 `.deb` 构建脚本，systemd、品牌图标、应用菜单、桌面快捷方式、主机/终端自启动和目标机验收脚本。
- Linux amd64/ARM64 离线轮子、两种架构的独立 CPython 3.11.15、SQLite/pysqlite3 源码与 SHA-256 清单。

## 验证摘要

- 2026-08-04 验证 UOS 基础安装的双架构离线依赖闭包：`numpy 2.2.6`、`onnxruntime 1.22.1`、`tokenizers 0.21.4` 与完整传递依赖均已纳入 amd64/ARM64 wheelhouse；两种架构各覆盖 43 个实际运行/构建依赖、包含 65 个离线文件，语义组件异常时仍可安全降级。
- 2026-08-04 修复四项推广级故障：设备心跳不再吞掉命令轮询；协同机更新成功后自动重启 Agent；升级异常中断遗留的失效锁可自动恢复；安装完成前强制验证更新助手与四个核心程序。
- 本轮执行 `.venv\\Scripts\\python.exe -m pytest backend/tests -q`：后端 159 项测试全部通过；既有覆盖率执行结果为 74.41%，不能把全绿测试误写成 90% 覆盖。
- 2026-08-04 复核两份项目体检报告并完成首轮认证加固：新增登录失败有界节流、HTTPS 安全 Cookie、首次配置本机限制与原子创建，演示数据改为显式启用；旧式只读备份令牌增加默认 365 天到期和自动撤销。逐条结论记录在 `docs/security-hardening-2026-08-04.md`。
- 前端 35 项 Vitest、4 项静态入口测试、类型检查和生产构建全部通过；全量语句/行覆盖率为 5.12%，页面自动化覆盖仍是正式推广门槛缺口。构建仅保留 UI 组件包超过 500KB 的性能提示，不影响离线运行和局域网功能。
- 应用内浏览器逐页验证 24 个主要业务路由，无页面级横向溢出和应用控制台错误；东方画卷固定定位、长条比例、透明层级和点击隔离均已检查。
- 浏览器回归在隔离 `.run-e2e` 主机完成：Ctrl+K、批量事项、智能文件夹、效率中心和帮助中心均可操作，证据截图位于 `output/playwright/efficiency-qa/`。
- `pip-audit` 与 `pnpm audit` 最终均未发现已知漏洞。
- 两个浏览器会话完成全闭环和实时协同；备份拉取、304、撤销、恢复、重启持久化通过。
- 10,000 条事项：首页 p95 443.69 ms，FTS5 搜索 p95 0.92 ms。
- 100,000 个文件索引：全文搜索 p95 1.15 ms，文件树首屏 p95 0.94 ms。
- amd64/ARM64 两套 Linux 离线轮子、独立 CPython 3.11.15、SQLite 3.51.3 与 pysqlite3 源码均写入 SHA-256 清单；两种 `.deb` 需在对应 UOS V20 目标机原生构建后复核。
- UOS build kit 包级检查通过：关键文件完整、无测试数据/缓存、Shell 脚本均为 LF；外层 ZIP 与同名 `.sha256` 一致。正式更新包生成后会再次执行 Ed25519、公钥、版本、双架构制品、大小和 SHA-256 自校验。
- UOS V20 amd64/ARM64 原生构建、双击安装、原位升级、20 台设备压力和灾备切换仍需在目标机执行后才能标记最终发布通过。
- 当前 `PartyOps-UOS-build-kit.zip` 已包含 PartyOps 1.3.4 最新源码、前端生产构建、双架构基础离线依赖、安装器和运维手册，SHA-256 与私钥泄露扫描通过；它是目标机原生构建套件，不是已经完成双架构验收的最终 `.deb` 发布目录。
- 本地语义重排和 llama.cpp 属于可选增强能力。套件缺少目标架构原生组件时会生成基础运行时并安全降级为规则推荐与已批准的外部 AI；若正式版本声明支持本地模型，须补齐 `BUILD-KIT-CAPABILITIES.txt` 列出的输入并设置 `PARTYOPS_REQUIRE_LOCAL_AI_RUNTIME=1` 后构建。

## 回滚

- 代码变更无 Git 仓库可用，不能提供提交级回滚；可用原始需求文档和最终构建套件重新生成。
- 业务数据变更通过 `.partyops-backup` 恢复；每次恢复前自动备份现状并在失败时回滚。
- UOS 升级失败时停止服务、安装上一版本，保留 `/var/lib/partyops`，必要时恢复升级前备份。

## 1.4.0 真协同与跨平台升级（2026-08-08）

### 前置说明

- 输入来源：四份 2026-08-05 体检/总纲报告、用户确认的《PartyOps 1.4.0 真协同与跨平台升级计划》及当前 1.3.4 工作区。
- 工具降级：当前会话未提供 Sequential Thinking、Context7、Fetch 与专门发布 MCP，实施使用本地 `rg`、安全 PowerShell、`apply_patch`、pytest/Vitest、Chrome 插件和官方公开文档；影响仅为审计载体不同，所有改动和验证仍落在工作区文件/日志中。回滚依赖上一版本安装包与 `.partyops-backup`，不能使用 Git 提交回退（`.git` 无可用历史）。
- 平台假设：本机为 Windows 11 x64；UOS amd64/arm64 和 Windows 10 真机由发布阶段提供。

### 变更摘要

- 新增 `0016` 迁移和统一档案/远端文件权限校验；协同人员按用户或设备获得查看、下载、贡献授权。
- 修复档案字段级错误丢失、动态字段类型、年度考核选项与协同机操作按钮。
- 完成共享目录重复管理、审批、目录授权、三向传输、远端物化、收件箱处理和非管理员协同页。
- 完成评论提及/回复、通知中心、顶栏角标和“我的工作”四类聚合。
- 完成显式 host/client 角色、Windows 服务/Agent/协议/防火墙、Windows 更新平台适配和 1.3.4 清单兼容。
- 依赖审计修复 `cryptography`、`nanoid` 和 `postcss` 已知漏洞。

### 验证与制品

- 后端 164 项、前端 35 项、类型检查和生产构建通过；依赖审计无已知漏洞。
- Windows 冻结运行时：1.4.0、SQLite 3.53.3、模式 `0016`；安装/卸载闭环通过。
- Windows 候选安装器：`artifacts/PartyOps_1.4.0_windows_amd64.exe`，85,490,512 字节，SHA-256 `f5fdf8cd249b62a95b1a35658939b075fa7b09a99e67a90522d711f5b2fee754`，未做 Authenticode 签名。
- Chrome 已完成管理员/普通账号的档案贡献、字段级错误、评论提及、通知跳转、层级回复、“我的工作”和非管理员协同页闭环；1.3.4/1.4.0 同状态视觉对照位于 `output/chrome-qa-1.4.0/`。
- 覆盖率未达门槛：后端语句 78%/行 73%，前端 4.87%。UOS 双架构、Windows 10、24 小时运行和正式签名也未完成，因此不生成或冒充正式统一更新包。

### 回滚

- Windows 卸载已验证移除服务、进程、防火墙、协议、自启和安装文件，保留 ProgramData/LocalAppData 业务数据。
- UOS 仍按上一版本同架构 DEB + 升级前 `.partyops-backup` 回滚；必须在目标机完成实测后签发。

## 1.4.1 真文件共享与本地智能候选（2026-08-09）

### 前置说明

- 输入：用户确认的《PartyOps 1.4.1 真文件共享与本地智能发布收口计划》、1.4.0 候选工作区和本轮 Chrome/Windows 11 x64 隔离环境。
- 工具降级：会话未提供 Sequential Thinking、Context7、Fetch 与发布签名 MCP；实施使用工作区 `rg`、安全 PowerShell、`apply_patch`、pytest/Vitest、Chrome 和官方公开资料。影响仅为调研/编辑载体不同；变更、日志、哈希和验收证据均保存在工作区。回滚使用候选卸载器、0017 降级或升级前备份。
- 不确定性：未获得 Windows 10、UOS amd64/arm64 真机、正式签名密钥和可再分发模型权重，故正式发布结论为 NO-GO。

### 交付说明

- 完成普通协同用户真实目录发布、团队/指定成员权限、能力驱动界面、浏览器/Agent 双通道下载、批量/文件夹 ZIP、逐分块权限复核与审计。
- 完成 embedding/LLM 分能力激活、BGE CLS 池化、正文目录授权和三架构 llama.cpp b10331 打包输入。
- 后端 170 项、前端 35 项、Sites 4 项、类型检查和构建通过；Windows 11 x64 安装、协议、下载与卸载闭环通过。
- Windows 候选：`PartyOps_1.4.1_windows_amd64.exe`，133,370,070 字节，SHA-256 `352dca19f70e4ca7097bff642f2fd000af87648a5669e7edbe9934c8e5256222`，未签名。
- UOS 构建套件：`PartyOps-UOS-build-kit.zip`，337,983,535 字节，SHA-256 `897139590050672f20c519dd5ed4eb3510cbb8f553dae8590ceef9a638673dcd`；它不是已验收 DEB。
- 覆盖率、跨平台真机、20GB/断线/24h、cryptography 上游修复与正式签名仍是阻断项，因此未生成占位 `.deb`、`.partyops-update` 或模型包。

### 回滚

- Windows 候选卸载已验证清除安装目录、服务、协议、自启、防火墙和快捷方式，保留业务数据。
- 0017 刚升级且未写入新业务时可降至 0016；其他情况恢复升级前 `.partyops-backup`。
- UOS 正式放行前必须在两种架构用上一版本 DEB 和升级前备份完成程序、数据库双回滚。
