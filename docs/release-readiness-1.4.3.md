# PartyOps 1.4.3-rc.7 发布就绪判定

最后核对：2026-08-19 15:50（北京时间，UTC+8）。
当前结论：七个平台安装包与本地质量门禁已通过，允许进入签名升级包、发布清单和远端上传阶段；线上验收仍未完成。

## 已通过

1. 七个平台安装包从产品提交 `a35a522b5e62093934e81d9098bd1e25fccb68ec` 构建并冻结，文件大小与 SHA-256 已写入制品清单。
2. 后端、前端、类型检查、生产构建、静态资源闭包、Windows 本机升级安装、Win7 PE 门禁和四个 Linux 最终包运行门禁通过。
3. 生产依赖漏洞、SAST、秘密和恶意软件扫描未发现发布阻断；SBOM 已生成。
4. 现场问题修复已纳入 rc.7：个人模式连接拒绝、旧进程版本不一致、Win7 缺失 Path API、Linux 安装后无法启动、模式切换回滚、旧服务管理程序缺失、事项删除与安装路径脚本编码问题。

## 发布顺序与阻断条件

1. 生成并逐个验证七个 Ed25519 签名更新包、更新目录、VEX 和机器可读发布清单。
2. 上传新的 CloudStudio 版本化目录并核验文件头、长度、完整下载哈希和 Range 206。
3. 推送 GitHub/Gitee `main` 与不可变标签 `v1.4.3-rc.7`，创建 GitHub 普通 Release（`prerelease=false`、`make_latest=false`）。
4. 部署 EdgeOne 项目 `makers-gjuf8qcecmi3`，再核验官网、HTTPS、移动端、下载、更新日志、常见问题、简历、二维码和全局下载计数。

任一资产出现签名失败、哈希不一致、静态资源缺失、依赖闭包不完整或安装后自检失败时，只阻断该资产，不得以警告替代失败。

## 公开限制与回滚

Windows 7 与国产 Linux 缺少对应真机，不宣称原生真机通过。Windows 安装器没有商业代码签名证书，可能触发 SmartScreen。应用更新失败使用包内回滚事务和升级前数据快照；官网可回滚 EdgeOne 页面部署，GitHub 历史 Release 用于审计与应急取回，官网只展示最新 rc.7 下载。
