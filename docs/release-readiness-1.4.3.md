# PartyOps 1.4.3-rc.7 发布就绪判定

最后核对：2026-08-19（北京时间，UTC+8）。当前结论：**rc.7 正在修复与对抗式审查，尚未冻结、上传或部署。任何 rc.6 制品哈希都不得冒充 rc.7。**

## rc.6 可继承但必须重新核对的历史证据

- 后端 912/912；追加真实分支探针后行覆盖率 95.15%、分支覆盖率 90.01%。前端覆盖率、类型检查、生产构建和静态资源闭包通过。
- 依赖锁一致性、`pip check`、pip-audit、前端/官网生产依赖审计、Bandit 高/中危和 gitleaks 通过；已知高危/严重漏洞为 0。
- rc.6 曾在当前 Win11 非 C 盘中文/空格目录完成覆盖安装。rc.7 改动了启动器、模式切换和业务功能，必须重新执行本地安装/升级验收后才能继承结论。
- Win7 x64/x86 从同一源码冻结并通过 PE/架构/资源/API 静态门禁；通用包阻止 Win7 误装，专用包拒绝 `api-ms-win-core-path-*` 导入。两个冻结运行时已在当前 Win11 启动验证，但尚未在 Win7 真机运行。
- 四个 Linux 原生包通过包头、架构、资源模式、严格依赖闭包、同架构回滚载荷与 glibc 2.17 运行时门禁；AMD64 的 DEB/RPM 与 ARM64 的 DEB/RPM 均从最终安装包本体解开并完成包自检、个人服务、健康端点和桌面启动器动态回归。目标商业系统仍未真机验证。
- rc.6 的签名、恶意软件扫描和线上结果只作为流程样板，不是 rc.7 放行证据。

## 线上放行条件

1. 先完成 rc.7 七个平台本地构建、安全门禁、最终制品反解验证与 Windows 本机安装验收。
2. 新 CloudStudio 部署核验后，只精确移除旧 rc.5 安装包，不删除下载计数数据或新部署。
3. GitHub 推送 `main` 文档/工具更新，创建不可变标签 `v1.4.3-rc.7`，并创建 `prerelease=false`、`make_latest=false` 的普通 Release；保留历史 Release 供审计。
4. EdgeOne 只部署到项目 `makers-gjuf8qcecmi3`。官网默认使用两步新手选包助手，只在系统和处理器类型明确后显示唯一 rc.7 安装包。发布时间统一为北京时间，并重新执行桌面/移动端、下载、更新日志、简历、二维码和全局计数回归。

## 公开限制与回滚

Windows 7 与国产 Linux 缺少对应真机，不宣称原生运行验收通过。Win7 仅建议在受控局域网使用，x86 不含语义重排和本地 LLM；rc.7 Windows 安装器仍没有商业代码签名证书。

应用更新失败使用包内回滚事务和升级前数据快照。官网可回滚 EdgeOne 页面部署，但 CloudStudio 按“只存最新安装包”的要求不保留旧下载源；GitHub 历史 Release 保留为审计与应急人工取回渠道。
