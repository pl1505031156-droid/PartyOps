# PartyOps 1.4.3-rc.8 发布就绪判定

最后核对：2026-08-19 20:31（北京时间，UTC+8）。
当前结论：七个平台安装包、本地质量门禁和签名更新包已通过，允许进入发布清单与远端上传阶段；线上验收仍未完成。

## 已通过

1. 七个平台安装包从产品提交 `bc0ad0c0e6eb4f272bcda731e52894dc4a282c48` 构建并冻结，文件大小与 SHA-256 已写入制品清单且与 `.sha256` 逐一一致。
2. 后端、前端、类型检查、生产构建、静态资源闭包、Windows 本机升级安装、Win7 PE 门禁和四个 Linux 最终包运行门禁通过。
3. Windows 本机 rc.7→rc.8 覆盖安装保持自定义 E 盘路径、业务库完整性和服务启动类型；个人模式健康、版本与单实例门禁通过。
4. Linux 安装后事务、固定共享运行时、桌面入口日志、提前退出诊断和版本不一致拦截通过。
5. 生产依赖漏洞、SAST、秘密和恶意软件扫描未发现发布阻断；SBOM、VEX 与 Ed25519 更新包已生成。

## 发布顺序与阻断条件

1. 生成并逐个验证七个 Ed25519 签名更新包、更新目录和机器可读发布清单。
2. 上传新的 CloudStudio 版本化目录并核验文件头、长度、完整下载哈希和 Range 206。
3. 推送 GitHub/Gitee `main` 与不可变标签 `v1.4.3-rc.8`，创建 GitHub 普通 Release（`prerelease=false`、`make_latest=false`）。
4. 部署 EdgeOne 项目 `makers-gjuf8qcecmi3`，再核验官网、HTTPS、移动端、下载、更新日志、常见问题、简历、二维码和全局下载计数。

任一资产出现签名失败、哈希不一致、静态资源缺失、依赖闭包不完整或安装后自检失败时，只阻断该资产，不得以警告替代失败。

## 公开限制与回滚

Windows 7 与国产 Linux 缺少对应真机，不宣称原生真机通过。Windows 安装器没有商业代码签名证书，可能触发 SmartScreen。应用更新失败使用包内回滚事务和升级前数据快照；官网可回滚 EdgeOne 页面部署，GitHub 历史 Release 用于审计与应急取回，官网只展示最新 rc.8 下载。
