# PartyOps 1.4.3-rc.3 发布就绪判定

最后核对：2026-08-17。当前结论：**冻结中；仅在五个可发布制品全部完成哈希、扫描与在线核验后转为 GO。rc.3 不是稳定版。**

## 放行矩阵

| 门禁 | 当前状态 | 放行条件 |
| --- | --- | --- |
| 后端测试 | 最新完整回归 886/886 通过 | 制品冻结后再跑一次完整回归并保持全绿 |
| 后端覆盖率 | 最新完整回归：行 95.23%、分支 90.04% | 冻结后仍须保持行、分支均不低于 90% |
| 前端/官网 | 前端 173/173，行 97.17%、分支 90.07%；官网 33/33，行 98.38%、分支 93.30%；前端生产构建通过 | 官网在冻结清单写入后执行生产构建；不得出现资源 404 或产品级控制台错误 |
| 高危漏洞/泄密 | 当前源码与锁文件门禁通过 | 对最终五个制品逐件重跑 SBOM、漏洞与恶意软件扫描 |
| Windows 10/11 | 安装器源码、静态门禁与旧候选自检通过；最终提交绑定包待构建 | 冻结运行时、本机静态/恶意软件扫描通过；真机运行结论按用户接受边界标为未验证 |
| Win7 x64/x86 | Legacy 锁与静态门禁存在，但依赖审计仍有高危项 | 本次明确阻断且不上传；取得可审计安全回移组件后另版发布 |
| Linux DEB/RPM | glibc 2.17 双架构 wheelhouse 各 66 个 wheel、62 个包；llama.cpp 与 Tesseract/OCR 双架构运行自检通过 | 生成四个原生包，完成静态安装后自检契约、制品扫描和哈希冻结 |
| 发布一致性 | 五包构建中 | 五包在 CloudStudio/GitHub 哈希一致，官网由冻结清单生成 |

## 不可降级的阻断项

1. 不得把分支覆盖率失败改成警告或降低阈值。
2. 不得用“重复 cryptography 已解决”替代 Linux 完整 ABI/依赖闭包结论。
3. 不得以旧 Win7 依赖加忽略规则代替安全回移补丁、复现测试和 VEX。
4. 不得把 Inno 语法探针当成产品安装包；探针不含正式运行时。
5. 不得在没有最终制品时预填大小、SHA-256 或上传时间。
6. GitHub `prerelease=false` 只是发布属性，不代表真机验证或稳定承诺。
7. rc.3 是应用内升级信任基线；rc.2 → rc.3 需一次手工原位桥接，后续平台升级必须通过受保护公钥、精确平台选包、一致性备份和失败回滚。

## 用户接受的发布限制

无真机不再是独立硬阻断项。依据用户 2026-08-14 的明确指令，新增平台可以在真实构建、静态兼容、自检、依赖闭包、覆盖率、安全和哈希门禁全部通过后发布；官网、GitHub Release、发布清单与安装器必须持续标注“未真机验证”。后续使用者反馈不能倒填本次验收记录，只能形成新的现场验证记录或修复版本。

## 发布顺序

全部门禁关闭后才执行：冻结干净提交与清单 → 构建五包 → 逐件本地验收/扫描 → CloudStudio 上传并验证 Range/Magic/SHA（平台不支持 Range 时如实记录 200）→ GitHub 普通 Release（`make_latest=false`）→ EdgeOne 项目 `makers-gjuf8qcecmi3` → 桌面与移动端回归。

回滚仅恢复旧官网部署和 rc.2 版本化链接；不删除历史资产，不移动标签，不删除用户数据。
