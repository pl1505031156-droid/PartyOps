# 党建智办 PartyOps 设计规范

## 设计方向

PartyOps 面向 UOS 桌面办公环境，采用“温润公文纸张 × 清晰责任标记”的编辑式界面。页面应克制、可信、可打印，不使用装饰性大屏、紫色渐变或无业务含义的动态图形。

## 东方四时长卷（1.3.3）

- 不改变既有导航、卡片、表格、按钮、间距和业务流程，只在内容层背后增加不接收点击的位图皮肤。
- 春、夏、秋、冬各提供页头远景、内容留白、空状态、分隔远山和底部水岸；卷宗、鸿雁、文房、流水作为页面语义素材。每个路由只选择一幅页头和一幅底景，绝不在同一位置叠加多张底图。
- 今日、日历、登录采用舒展构图；任务、收件、汇总、日志、专题、文件、档案、知识、协同和管理页面按业务语义选择不同组合，使相邻页面的景物、视觉重心和留白关系不重复。
- 页头画卷横向铺展在标题右侧整段留白，不能缩成右上角小景。底部长卷固定于视口下沿并位于业务层后方；卡片和表格采用温润半透明纸张底，使画卷透出且文字、边框、按钮、输入框和敏感正文保持清晰。
- 季节按照节气边界自动切换春夏秋冬；节气名称只在交节当天显示，东方画景的重心、舒展度等微变量则在当前约十五天节气周期内持续生效，直到下一节气切换；节气不增加独立叠图。
- 所有山水、花木与空状态图片必须保持源文件宽高比。页头与空状态使用 `background-size: contain` 或等价的 `object-fit: contain`；底部长卷使用 `background-size: 100% auto`，以主内容区全宽为基准等比缩放，高度由源图比例自然得出。禁止 `100% 100%`、非等比缩放或分别改写宽高来填满槽位。
- 底部长卷的画布必须从主内容区左边缘连续延伸到右边缘，统一使用 `left: 0`、`right: 0`、`background-position: center bottom`；节气微变量只允许改变纵向位置和等比缩放，不得横移画卷而露出断边。源图内部可以保留构图留白，但不得把整幅画缩成页面中央的小块。
- 空状态插画、档案物件和角落植物不得作为全宽底图放大。原始文件中心、重要档案和管理域（含使用帮助）使用同季横向水岸长卷承接底部留白，页面语义由各自页头表达；管理域页头与底景采用克制尺寸，避免装饰压过表单、表格和诊断信息。
- 今日、日历和登录页显示完整“公历＋星期＋农历＋节气”，其余页面只在既有页头副文案中显示简化题签。
- 任务、收件、周期汇总、工作日志、专题、知识、设备协同和文件传输均使用独立语义构图，并各自提供春夏秋冬四个版本；同一页面在季节切换时保留构图身份，只改变色候、物候和植被，不复用另一页面的底景。
- 页面专属画面必须落在该页面真实安全留白中：页头景避开标题、帮助和主操作，底景避开表格、时间线、录入区和分页。长列表没有安全留白时允许底景位于首屏以下，不得为了“看见装饰”穿过业务内容。
- 页头和底景必须在统一主题层进行横向羽化，墨色在进入宣纸留白前自然消散；禁止让素材 Alpha 边界形成可见的垂直切线、矩形贴图边框或突然截断。羽化只能影响透明通道，不得用非等比缩放掩盖断边。
- 靠右或靠左的画面主体必须贴合主内容区对应边界；源图透明装裱边应移出视口，不能在边界前留下空白竖条。只有中央构图允许双侧渐隐。
- 管理域前七个页面是上述贴边规则的明确例外：页头画卷固定在标题右侧、操作按钮左侧的中央安全留白，桌面宽屏约使用主内容区 `31%—87%` 的横向范围并双侧渐隐，右侧必须给“本页帮助 / 刷新 / 助手”保留独立操作区。
- 迎检、原始文件、重要档案与管理域的大块留白统一使用横向长卷承接，不得把中幅插画或角落物件放大成孤立背景块；页面语义差异由页头构图、透明度和安全位置表达。
- 逐页视觉验收以任务、日历、汇总、日志、收件、专题、文件、档案、迎检、知识、文档比较、设备协同和文件传输 13 个场景为基准；每页都必须与对应参考图并排检查，禁止只验收首页。
- 根节点仅通过 `data-season`、`data-art-level`、`data-art-profile`、`data-scene`、`data-solar-term`、`data-reduce-motion` 控制皮肤，业务页面不得散落季节判断或素材路径。
- 标准装饰只有 140—180ms 淡入；减少装饰时隐藏画面但保留日期文字，减少动态时关闭非必要过渡。

## 颜色

| 用途 | 颜色 |
| --- | --- |
| 页面底色 | `#f7f1e7` |
| 卡片底色 | `#fffaf1` |
| 朱红主色 | `#b42318` |
| 深朱红 | `#8f1d14` |
| 炭黑正文 | `#292520` |
| 暖灰次文 | `#746d64` |
| 纸张边线 | `#d8ccbc` |
| 完成状态 | `#2f7d57` |
| 风险状态 | `#c17b17` |
| 错误状态 | `#b42318` |

状态不得只依靠颜色表达，必须同时显示文字或图标。

## 字体与密度

- 品牌和页面大标题使用随包中文宋体或 `Noto Serif CJK SC`。
- 正文、表格和表单使用系统中文无衬线字体。
- 页面标题 28—34px，模块标题 18—22px，正文 14px，辅助文字 12px。
- 基础间距为 4px，常用组合为 8、12、16、24、32px。
- 桌面主要按钮高度 36—40px，行高不低于 1.6。

## 页面骨架

- 一级导航固定为“今日、工作、资料、协同、管理”。
- 页面内容最大宽度根据业务自适应，常规表单和阅读内容控制在 1280px 内。
- 1366×768 下主操作不产生横向滚动；宽表格可在表格自身区域滚动。
- 加载、空数据、部分失败、离线、权限不足、版本冲突和后台处理中必须有稳定页面状态，禁止空白页。

## 弹窗

- 弹窗固定居中，宽度不超过 `calc(100vw - 48px)`，高度不超过 `calc(100vh - 48px)`。
- 标题和底部操作区固定，正文区域独立滚动。
- 高风险操作说明影响范围并要求二次确认。
- 默认主操作位于右侧，取消操作位于左侧；键盘 `Esc` 关闭非强制弹窗。

## 交互

- `Ctrl+K` 支持搜索和高频指令。
- 主按钮统一朱红实底，次按钮为暖纸透明底和细边框，圆角固定为 2px；禁止新增蓝色主按钮或大圆角胶囊按钮。
- 勾选框、单选框、开关和分段选择统一使用朱红选中态、暖灰未选中态，并保留清晰的键盘焦点环。
- 新增页面优先复用 Arco Design Vue 现有控件及全局主题，不在页面内另造一套按钮、表单或选择器。
- 正式截止日期不能通过日历拖拽静默修改；修改必须进入任务表单并记录原因。
- 所有异步操作显示进度、中文错误和诊断编号。
- 同一业务对象只保存一份，列表、看板、日历、报告和专题均为其不同视图。
