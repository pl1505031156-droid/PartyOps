diff --git a/pyproject.toml b/pyproject.toml
index 8f35a20..b997d8f 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -4,12 +4,12 @@ build-backend = "hatchling.build"
 
 [project]
 name = "starlette"
-dynamic = ["version"]
+version = "1.3.1+partyops.1"
 description = "The little ASGI library that shines."
 readme = "README.md"
 license = "BSD-3-Clause"
 license-files = ["LICENSE.md"]
-requires-python = ">=3.10"
+requires-python = ">=3.8"
 authors = [
     { name = "Tom Christie", email = "tom@tomchristie.com" }
 ]
@@ -79,9 +79,6 @@ Changelog = "https://starlette.dev/release-notes/"
 Funding = "https://github.com/sponsors/Kludex"
 Source = "https://github.com/Kludex/starlette"
 
-[tool.hatch.version]
-path = "starlette/__init__.py"
-
 [tool.ruff]
 line-length = 120
 
diff --git a/starlette/__init__.py b/starlette/__init__.py
index 9c73af2..c758266 100644
--- a/starlette/__init__.py
+++ b/starlette/__init__.py
@@ -1 +1 @@
-__version__ = "1.3.1"
+__version__ = "1.3.1+partyops.1"
diff --git a/starlette/_exception_handler.py b/starlette/_exception_handler.py
index 9491f8b..06d2c85 100644
--- a/starlette/_exception_handler.py
+++ b/starlette/_exception_handler.py
@@ -1,6 +1,6 @@
 from __future__ import annotations
 
-from typing import Any
+from typing import Any, Dict
 
 from starlette._utils import is_async_callable
 from starlette.concurrency import run_in_threadpool
@@ -9,8 +9,8 @@ from starlette.requests import Request
 from starlette.types import ASGIApp, ExceptionHandler, Message, Receive, Scope, Send
 from starlette.websockets import WebSocket
 
-ExceptionHandlers = dict[Any, ExceptionHandler]
-StatusHandlers = dict[int, ExceptionHandler]
+ExceptionHandlers = Dict[Any, ExceptionHandler]
+StatusHandlers = Dict[int, ExceptionHandler]
 
 
 def _lookup_exception_handler(exc_handlers: ExceptionHandlers, exc: Exception) -> ExceptionHandler | None:
diff --git a/starlette/_utils.py b/starlette/_utils.py
index b9b8639..6fe485b 100644
--- a/starlette/_utils.py
+++ b/starlette/_utils.py
@@ -2,9 +2,8 @@ from __future__ import annotations
 
 import functools
 import sys
-from collections.abc import AsyncGenerator, Awaitable, Callable, Generator
-from contextlib import AbstractAsyncContextManager, asynccontextmanager
-from typing import Any, Generic, Protocol, TypeVar, overload
+from contextlib import asynccontextmanager
+from typing import Any, AsyncContextManager, AsyncGenerator, Awaitable, Callable, Generator, Generic, Protocol, TypeVar, overload
 
 import anyio.abc
 
@@ -50,7 +49,7 @@ T_co = TypeVar("T_co", covariant=True)
 
 
 class AwaitableOrContextManager(
-    Awaitable[T_co], AbstractAsyncContextManager[T_co], Protocol[T_co]
+    Awaitable[T_co], AsyncContextManager[T_co], Protocol[T_co]
 ): ...  # pragma: no branch
 
 
diff --git a/starlette/applications.py b/starlette/applications.py
index 3e1a781..b17631f 100644
--- a/starlette/applications.py
+++ b/starlette/applications.py
@@ -1,7 +1,9 @@
 from __future__ import annotations
 
 from collections.abc import Awaitable, Callable, Mapping, Sequence
-from typing import Any, ParamSpec, TypeVar
+from typing import Any, TypeVar
+
+from typing_extensions import ParamSpec
 
 from starlette.datastructures import State, URLPath
 from starlette.middleware import Middleware, _MiddlewareFactory
diff --git a/starlette/authentication.py b/starlette/authentication.py
index 63c4da0..3ec989d 100644
--- a/starlette/authentication.py
+++ b/starlette/authentication.py
@@ -3,7 +3,9 @@ from __future__ import annotations
 import functools
 import inspect
 from collections.abc import Callable, Sequence
-from typing import Any, ParamSpec
+from typing import Any
+
+from typing_extensions import ParamSpec
 from urllib.parse import urlencode
 
 from starlette._utils import is_async_callable
diff --git a/starlette/background.py b/starlette/background.py
index 55135ee..b93ee21 100644
--- a/starlette/background.py
+++ b/starlette/background.py
@@ -1,7 +1,9 @@
 from __future__ import annotations
 
 from collections.abc import Callable, Sequence
-from typing import Any, ParamSpec
+from typing import Any
+
+from typing_extensions import ParamSpec
 
 from starlette._utils import is_async_callable
 from starlette.concurrency import run_in_threadpool
diff --git a/starlette/concurrency.py b/starlette/concurrency.py
index a2c55be..ddf304b 100644
--- a/starlette/concurrency.py
+++ b/starlette/concurrency.py
@@ -3,7 +3,9 @@ from __future__ import annotations
 import functools
 import warnings
 from collections.abc import AsyncIterator, Callable, Coroutine, Iterable, Iterator
-from typing import ParamSpec, TypeVar
+from typing import TypeVar
+
+from typing_extensions import ParamSpec
 
 import anyio.to_thread
 
diff --git a/starlette/config.py b/starlette/config.py
index 56c25ca..8a79658 100644
--- a/starlette/config.py
+++ b/starlette/config.py
@@ -2,9 +2,8 @@ from __future__ import annotations
 
 import os
 import warnings
-from collections.abc import Callable, Iterator, Mapping, MutableMapping
 from pathlib import Path
-from typing import Any, TypeVar, overload
+from typing import Any, Callable, Iterator, Mapping, MutableMapping, TypeVar, overload
 
 
 class undefined:
diff --git a/starlette/datastructures.py b/starlette/datastructures.py
index 33ecb1b..2929ddd 100644
--- a/starlette/datastructures.py
+++ b/starlette/datastructures.py
@@ -1,9 +1,24 @@
 from __future__ import annotations
 
 import re
-from collections.abc import ItemsView, Iterable, Iterator, KeysView, Mapping, MutableMapping, Sequence, ValuesView
 from shlex import shlex
-from typing import Any, BinaryIO, Literal, NamedTuple, TypeVar, cast
+from typing import (
+    Any,
+    BinaryIO,
+    ItemsView,
+    Iterable,
+    Iterator,
+    KeysView,
+    Literal,
+    Mapping,
+    MutableMapping,
+    NamedTuple,
+    Sequence,
+    TypeVar,
+    Union,
+    ValuesView,
+    cast,
+)
 from urllib.parse import SplitResult, parse_qsl, urlencode, urlsplit
 
 from starlette.concurrency import run_in_threadpool
@@ -479,7 +494,7 @@ class UploadFile:
         return f"{self.__class__.__name__}(filename={self.filename!r}, size={self.size!r}, headers={self.headers!r})"
 
 
-class FormData(ImmutableMultiDict[str, UploadFile | str]):
+class FormData(ImmutableMultiDict[str, Union[UploadFile, str]]):
     """
     An immutable multidict, containing both file uploads and text input.
     """
diff --git a/starlette/middleware/__init__.py b/starlette/middleware/__init__.py
index f8d47d7..cc1daae 100644
--- a/starlette/middleware/__init__.py
+++ b/starlette/middleware/__init__.py
@@ -1,7 +1,8 @@
 from __future__ import annotations
 
-from collections.abc import Awaitable, Callable, Iterator
-from typing import Any, ParamSpec, Protocol
+from typing import Any, Awaitable, Callable, Iterator, Protocol
+
+from typing_extensions import ParamSpec
 
 P = ParamSpec("P")
 
diff --git a/starlette/middleware/base.py b/starlette/middleware/base.py
index 2579c03..0cf6acd 100644
--- a/starlette/middleware/base.py
+++ b/starlette/middleware/base.py
@@ -1,7 +1,6 @@
 from __future__ import annotations
 
-from collections.abc import AsyncGenerator, AsyncIterable, Awaitable, Callable, Mapping, MutableMapping
-from typing import Any, TypeVar
+from typing import Any, AsyncGenerator, AsyncIterable, Awaitable, Callable, Mapping, MutableMapping, TypeVar, Union
 
 import anyio
 
@@ -12,8 +11,10 @@ from starlette.types import ASGIApp, Message, Receive, Scope, Send
 
 RequestResponseEndpoint = Callable[[Request], Awaitable[Response]]
 DispatchFunction = Callable[[Request, RequestResponseEndpoint], Awaitable[Response]]
-BodyStreamGenerator = AsyncGenerator[bytes | MutableMapping[str, Any], None]
-AsyncContentStream = AsyncIterable[str | bytes | memoryview | MutableMapping[str, Any]]
+BodyStreamGenerator = AsyncGenerator[Union[bytes, MutableMapping[str, Any]], None]
+AsyncContentStream = AsyncIterable[
+    Union[str, bytes, memoryview, MutableMapping[str, Any]]
+]
 T = TypeVar("T")
 
 
diff --git a/starlette/middleware/sessions.py b/starlette/middleware/sessions.py
index aa0383f..aa5bbed 100644
--- a/starlette/middleware/sessions.py
+++ b/starlette/middleware/sessions.py
@@ -88,7 +88,7 @@ class SessionMiddleware:
         await self.app(scope, receive, send_wrapper)
 
 
-class Session(dict[str, typing.Any]):
+class Session(dict):
     accessed: bool = False
     modified: bool = False
 
diff --git a/starlette/requests.py b/starlette/requests.py
index d4f3f4b..8472574 100644
--- a/starlette/requests.py
+++ b/starlette/requests.py
@@ -2,9 +2,8 @@ from __future__ import annotations
 
 import json
 import sys
-from collections.abc import AsyncGenerator, Iterator, Mapping
 from http import cookies as http_cookies
-from typing import TYPE_CHECKING, Any, Generic, NoReturn, cast
+from typing import TYPE_CHECKING, Any, AsyncGenerator, Generic, Iterator, Mapping, NoReturn, Union, cast
 
 import anyio
 
@@ -74,7 +73,7 @@ class ClientDisconnect(Exception):
     pass
 
 
-StateT = TypeVar("StateT", bound=Mapping[str, Any] | State, default=State)
+StateT = TypeVar("StateT", bound=Union[Mapping[str, Any], State], default=State)
 
 
 class HTTPConnection(Mapping[str, Any], Generic[StateT]):
diff --git a/starlette/responses.py b/starlette/responses.py
index 63b49ec..ee75a3a 100644
--- a/starlette/responses.py
+++ b/starlette/responses.py
@@ -6,13 +6,12 @@ import json
 import os
 import stat
 import sys
-from collections.abc import AsyncIterable, Awaitable, Callable, Iterable, Mapping, Sequence
 from datetime import datetime
 from email.utils import format_datetime, formatdate
 from functools import partial
 from mimetypes import guess_type
 from secrets import token_hex
-from typing import Any, Literal
+from typing import Any, AsyncIterable, Awaitable, Callable, Iterable, Literal, Mapping, Sequence, Union
 from urllib.parse import quote
 
 import anyio
@@ -213,10 +212,10 @@ class RedirectResponse(Response):
         self.headers["location"] = quote(str(url), safe=":/%#?=@[]!$&'()*+,;")
 
 
-Content = str | bytes | memoryview
+Content = Union[str, bytes, memoryview]
 SyncContentStream = Iterable[Content]
 AsyncContentStream = AsyncIterable[Content]
-ContentStream = AsyncContentStream | SyncContentStream
+ContentStream = Union[AsyncContentStream, SyncContentStream]
 
 
 class StreamingResponse(Response):
diff --git a/starlette/routing.py b/starlette/routing.py
index a035d18..5e80c02 100644
--- a/starlette/routing.py
+++ b/starlette/routing.py
@@ -8,10 +8,10 @@ import traceback
 import types
 import warnings
 from collections.abc import Awaitable, Callable, Collection, Generator, Sequence
-from contextlib import AbstractAsyncContextManager, AbstractContextManager, asynccontextmanager
+from contextlib import AbstractContextManager, asynccontextmanager
 from enum import Enum
 from re import Pattern
-from typing import Any, TypeVar
+from typing import Any, AsyncContextManager, TypeVar
 
 from starlette._exception_handler import wrap_app_handling_exceptions
 from starlette._utils import get_route_path, is_async_callable
@@ -521,7 +521,7 @@ class Host(BaseRoute):
 _T = TypeVar("_T")
 
 
-class _AsyncLiftContextManager(AbstractAsyncContextManager[_T]):
+class _AsyncLiftContextManager(AsyncContextManager[_T]):
     def __init__(self, cm: AbstractContextManager[_T]):
         self._cm = cm
 
@@ -554,10 +554,10 @@ class _DefaultLifespan:
         self._router = router
 
     async def __aenter__(self) -> None:
-        pass
+        await self._router.startup()
 
     async def __aexit__(self, *exc_info: object) -> None:
-        pass
+        await self._router.shutdown()
 
     def __call__(self: _T, app: object) -> _T:
         return self
@@ -569,6 +569,8 @@ class Router:
         routes: Sequence[BaseRoute] | None = None,
         redirect_slashes: bool = True,
         default: ASGIApp | None = None,
+        on_startup: Sequence[Callable[[], Any]] | None = None,
+        on_shutdown: Sequence[Callable[[], Any]] | None = None,
         # the generic to Lifespan[AppType] is the type of the top level application
         # which the router cannot know statically, so we use Any
         lifespan: Lifespan[Any] | None = None,
@@ -578,6 +580,11 @@ class Router:
         self.routes = [] if routes is None else list(routes)
         self.redirect_slashes = redirect_slashes
         self.default = self.not_found if default is None else default
+        self.on_startup = [] if on_startup is None else list(on_startup)
+        self.on_shutdown = [] if on_shutdown is None else list(on_shutdown)
+
+        if lifespan is not None and (self.on_startup or self.on_shutdown):
+            raise ValueError("lifespan 不能与 on_startup/on_shutdown 同时配置")
 
         if lifespan is None:
             self.lifespan_context: Lifespan[Any] = _DefaultLifespan(self)
@@ -626,6 +633,22 @@ class Router:
                 pass
         raise NoMatchFound(name, path_params)
 
+    async def startup(self) -> None:
+        """兼容 FastAPI Legacy：依次执行已注册的启动处理器。"""
+        for handler in self.on_startup:
+            if is_async_callable(handler):
+                await handler()
+            else:
+                handler()
+
+    async def shutdown(self) -> None:
+        """兼容 FastAPI Legacy：依次执行已注册的关闭处理器。"""
+        for handler in self.on_shutdown:
+            if is_async_callable(handler):
+                await handler()
+            else:
+                handler()
+
     async def lifespan(self, scope: Scope, receive: Receive, send: Send) -> None:
         """
         Handle ASGI lifespan messages, which allows us to manage application
diff --git a/starlette/staticfiles.py b/starlette/staticfiles.py
index ac39acc..7fcff1c 100644
--- a/starlette/staticfiles.py
+++ b/starlette/staticfiles.py
@@ -210,7 +210,10 @@ class StaticFiles:
         if if_none_match := request_headers.get("if-none-match"):
             # The "etag" header is added by FileResponse, so it's always present.
             etag = response_headers["etag"]
-            return etag in [tag.strip().removeprefix("W/") for tag in if_none_match.split(",")]
+            return etag in [
+                tag.strip()[2:] if tag.strip().startswith("W/") else tag.strip()
+                for tag in if_none_match.split(",")
+            ]
 
         try:
             if_modified_since = parsedate(request_headers["if-modified-since"])
diff --git a/starlette/testclient.py b/starlette/testclient.py
index 63d8790..a3d52cd 100644
--- a/starlette/testclient.py
+++ b/starlette/testclient.py
@@ -7,11 +7,26 @@ import json
 import math
 import sys
 import warnings
-from collections.abc import Awaitable, Callable, Generator, Iterable, Mapping, MutableMapping, Sequence
 from concurrent.futures import Future
-from contextlib import AbstractContextManager
 from types import GeneratorType
-from typing import TYPE_CHECKING, Any, Literal, TypedDict, TypeGuard, cast
+from typing import (
+    TYPE_CHECKING,
+    Any,
+    Awaitable,
+    Callable,
+    ContextManager,
+    Generator,
+    Iterable,
+    Literal,
+    Mapping,
+    MutableMapping,
+    Sequence,
+    TypedDict,
+    Union,
+    cast,
+)
+
+from typing_extensions import TypeGuard
 from urllib.parse import unquote, urljoin
 
 import anyio
@@ -50,14 +65,14 @@ else:
                 stacklevel=2,
             )
 
-_PortalFactoryType = Callable[[], AbstractContextManager[anyio.abc.BlockingPortal]]
+_PortalFactoryType = Callable[[], ContextManager[anyio.abc.BlockingPortal]]
 
 ASGIInstance = Callable[[Receive, Send], Awaitable[None]]
 ASGI2App = Callable[[Scope], ASGIInstance]
 ASGI3App = Callable[[Scope, Receive, Send], Awaitable[None]]
 
 
-_RequestData = Mapping[str, str | Iterable[str] | bytes]
+_RequestData = Mapping[str, Union[str, Iterable[str], bytes]]
 
 
 def _is_asgi3(app: ASGI2App | ASGI3App) -> TypeGuard[ASGI3App]:
diff --git a/starlette/types.py b/starlette/types.py
index b767ae1..8740402 100644
--- a/starlette/types.py
+++ b/starlette/types.py
@@ -1,6 +1,4 @@
-from collections.abc import Awaitable, Callable, Mapping, MutableMapping
-from contextlib import AbstractAsyncContextManager
-from typing import TYPE_CHECKING, Any, TypeVar
+from typing import TYPE_CHECKING, Any, AsyncContextManager, Awaitable, Callable, Mapping, MutableMapping, TypeVar, Union
 
 if TYPE_CHECKING:
     from starlette.requests import Request
@@ -17,10 +15,10 @@ Send = Callable[[Message], Awaitable[None]]
 
 ASGIApp = Callable[[Scope, Receive, Send], Awaitable[None]]
 
-StatelessLifespan = Callable[[AppType], AbstractAsyncContextManager[None]]
-StatefulLifespan = Callable[[AppType], AbstractAsyncContextManager[Mapping[str, Any]]]
-Lifespan = StatelessLifespan[AppType] | StatefulLifespan[AppType]
+StatelessLifespan = Callable[[AppType], AsyncContextManager[None]]
+StatefulLifespan = Callable[[AppType], AsyncContextManager[Mapping[str, Any]]]
+Lifespan = Union[StatelessLifespan[AppType], StatefulLifespan[AppType]]
 
 HTTPExceptionHandler = Callable[["Request", Exception], "Response | Awaitable[Response]"]
 WebSocketExceptionHandler = Callable[["WebSocket", Exception], Awaitable[None]]
-ExceptionHandler = HTTPExceptionHandler | WebSocketExceptionHandler
+ExceptionHandler = Union[HTTPExceptionHandler, WebSocketExceptionHandler]
