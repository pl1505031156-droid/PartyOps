# Windows 7 Python 3.8 Legacy 上游基线。
#
# 该输入仅用于解析可重复依赖树；cryptography 与 Pillow 的上游基线存在
# 2026 年安全缺口，正式构建必须由 security-backports.json 中的已验证
# PartyOps 回移轮子覆盖，否则 validate-win7-wheelhouse.py 必须阻断制品。
fastapi==0.115.14+partyops.1
starlette==1.3.1+partyops.1
uvicorn<0.31
SQLAlchemy<2.1
# SQLAlchemy 在 Windows 同步运行时会动态依赖 greenlet；必须显式锁定，
# 避免 x86 解析器漏写传递依赖后由旧 pip 在安装期临时联网解析。
greenlet==3.1.1
alembic<1.15
pydantic-settings<2.8
# Pydantic 在 Python 3.8 中求值源码里的 ``Path | None`` 等 3.10 类型标注时
# 需要该官方兼容层；缺少时应用在导入 Settings 阶段即退出。
eval-type-backport==0.4.0
argon2-cffi<24
sse-starlette<2.2
python-docx<1.2
lxml==6.1.1
PyMuPDF<1.25
openpyxl==3.1.5
# 0.3.2 是仍提供 CPython 3.8 双架构轮子的最后一代兼容线；仅用于 Win7
# 台账容器解析，主线系统继续使用经过完整门禁的新版 0.8.2。
python-calamine==0.3.2
Pillow==12.3.0+partyops.1
pytesseract==0.3.13
# 上游安全版本已停止支持 Python 3.8；正式 wheel 必须由
# security-backports.json 指定的 PartyOps 回移版本覆盖。
cryptography==50.0.0+partyops.1
defusedxml==0.7.1
httpx<0.28
packaging==25.0
click==8.3.3+partyops.1
python-dotenv==1.2.2+partyops.1
python-multipart==0.0.31+partyops.1
requests==2.33.0+partyops.1
urllib3==2.7.0+partyops.1
